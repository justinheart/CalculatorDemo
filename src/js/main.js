function onSignKeyMouseOver(element) {
  element.style.backgroundColor = "#ffe4e1"; // mistyrose
}

function onSignKeyMouseOut(element) {
  element.style.backgroundColor = "#ffa07a"; // lightsalmon
}

function onNumberKeyMouseOver(element) {
  element.style.backgroundColor = "#f5f5dc"; // beige
}

function onNumberKeyMouseOut(element) {
  element.style.backgroundColor = "#f0e68c"; // khaki
}

function onResultFieldMouseOver(element) {
  element.style.backgroundColor = "#f5fffa"; // mintcream
}

function onResultFieldMouseOut(element) {
  element.style.backgroundColor = "#ffffff"; // white
}